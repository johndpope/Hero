name: flutter_hero_transitions
description: >
  A powerful, declarative library for building custom view transitions in Flutter.
  Port of the iOS Hero library with 100% example parity.
version: 1.0.0
homepage: https://github.com/HeroTransitions/Hero

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
